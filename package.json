{
  "name": "@kubeploy/workspace",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "prepublish": "npm run build",
    "prepare": "husky"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.4.4",
    "typescript": "5.8.3"
  },
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=22"
  },
  "volta": {
    "node": "22.16.0"
  },
  "dependencies": {
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7"
  }
}

query GetWorkspaces($first: Int) {
  workspaces(first: $first) {
    edges {
      node {
        id
        ...Workspace
      }
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
